# Policy Benchmarks

This directory stores A/B policy comparison outputs generated by `save_policy_benchmark_result`.

- One JSON file per benchmark run (`<run_id>.json`).
- Keep historical files for manual review, regression triage, and rollback decisions.
