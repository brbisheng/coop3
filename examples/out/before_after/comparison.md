# Before vs After comparison placeholder

Run `python demos/demo_before_after.py --model <openrouter-model> --api-key <key>` to generate a live comparison between:

- baseline single-shot answer
- phase-1 rigor pipeline final output

The generated comparison includes:

- actor/node coverage
- mechanism depth
- competing predictions
- hidden assumptions
- surprise usefulness
